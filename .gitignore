# Dependencias
node_modules/

# Build
dist/

# Prisma — código generado (se regenera con yarn prisma:generate)
src/generated/

# Variables de entorno — NUNCA subir al repo
.env
.env.*
!.env.example
!infra/.env.production.example

# Yarn
.yarn/cache
.yarn/install-state.gz

# Logs
*.log

# OS
.DS_Store

# Uploads locales (estrategia local de upload — los archivos no se commitean)
uploads/*
!uploads/.gitkeep
